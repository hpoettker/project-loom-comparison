plugins {
  id 'org.springframework.boot' version '2.6.8' apply false
  id 'io.spring.dependency-management' version '1.0.11.RELEASE' apply false
}

subprojects {
  apply plugin: 'loomtest.conventions'

  ext.set('jetty.version', '10.0.9')
}